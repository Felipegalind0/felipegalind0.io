# fg.io — bento grid personal dashboard

A minimal, minimal-JS personal dashboard built with [Astro](https://astro.build). Mostly static HTML/CSS + 2 tiny inline scripts and one YouTube iframe, system-theme-aware (light/dark), deployed as a static site 2 GitHub Pages.

**Live:** [felipegalind0.io](https://felipegalind0.io)

---

## Layout

Diagrams are generated by [scripts/update-layout.py](scripts/update-layout.py) from [src/pages/index.astro](src/pages/index.astro). Run `python scripts/update-layout.py` 2 refresh; it prints parsed placements + source order.

Breakpoints: 1 column <580px, 2 columns 580px+, 3 columns 900px+, 4 columns 1200px+.

<!-- LAYOUT:START -->
**4 columns** (desktop, 1200px+)

```
┌────────────────┬────────────────┬────────────────┬────────────────┐
│  Felipe_Galind0_w               │  yt_playlist_w                │
│  [A]                            │  [B]                          │
├────────────────┬────────────────┼────────────────┼────────────────┤
│                │                │  gh_stats_w    │  blog_w        │
│                │                │  [C]           │  [D]           │
├────────────────┴────────────────┴────────────────┴────────────────┤
│  footer_w                                                           │
│  [E]                                                                │
└──────────────────────────────────────────────────────────────────────┘
```

**3 columns** (wide, 900px+)

```
┌────────────────┬────────────────┬────────────────┐
│  Felipe_Galind0_w               │  yt_playlist_w │
│  [A]            │  [B]                          │
├────────────────┼────────────────┼────────────────┤
│                │  gh_stats_w    │  blog_w        │
│                │  [C]           │  [D]           │
├────────────────┴────────────────┴────────────────┤
│  footer_w                                       │
│  [E]                                            │
└──────────────────────────────────────────────────┘
```

**2 columns** (tablet, 580px+)

```
┌────────────────────┬────────────────────┐
│  Felipe_Galind0_w  │  gh_stats_w        │
│  [A]               │  [C]               │
├────────────────────┴────────────────────┤
│  yt_playlist_w                            │
│  [B]                                      │
├────────────────────┬────────────────────┤
│  blog_w            │  footer_w          │
│  [D]               │  [E]               │
└────────────────────┴────────────────────┘
```

**1 column** (mobile)

```
┌────────────────────────────────────────┐
│  Felipe_Galind0_w                       │
│  [A]                                    │
├────────────────────────────────────────┤
│  gh_stats_w                             │
│  [C]                                    │
├────────────────────────────────────────┤
│  yt_playlist_w                          │
│  [B]                                    │
├────────────────────────────────────────┤
│  blog_w                                 │
│  [D]                                    │
├────────────────────────────────────────┤
│  footer_w                               │
│  [E]                                    │
└────────────────────────────────────────┘
```

Source order: A → C → B → D → E
<!-- LAYOUT:END -->

---

## Project Structure

```
fg.io/
├── public/
│   ├── CNAME                    # Custom domain 4 GitHub Pages
│   └── robots.txt               # Allow all bots
├── scripts/
│   ├── inject-size.mjs          # Post-build: replaces __SIZE__ in dist/index.html
│   ├── update-layout.py         # Generates ASCII layout diagrams 4 README
│   ├── scrape-polycam.py        # Playwright scraper 4 Polycam profile
│   └── upload-to-youtube.py     # Batch upload Polycam videos 2 YouTube
├── src/
│   ├── components/
│   │   ├── StatusCard.astro     # Widget [A]: bio, avatar, accomplishments
│   │   ├── GithubStats.astro    # Widget [C]: GitHub stats SVG image
│   │   └── PolycamGallery.astro # Widget [B]: YouTube playlist iframe + tiny JS
│   ├── data/
│   │   └── polycam.json         # Polycam manifest 4 scripts (not wired 2 site)
│   ├── layouts/
│   │   └── Layout.astro         # HTML shell, global reset, scanline overlay + HTTPS redirect
│   ├── pages/
│   │   └── index.astro          # Bento grid layout with 5 cells
│   ├── styles/
│   │   └── tokens.css           # CSS custom properties (design tokens)
│   └── config.ts                # VERSION constant
├── priv/                        # Gitignored secrets + polycam cache
│   └── polycam/                 # Local Polycam video/thumbnail cache
├── astro.config.mjs             # Astro config (site URL)
├── package.json
└── tsconfig.json
```

---

## Architecture

### Design Tokens (`src/styles/tokens.css`)

All theming is driven by CSS custom properties. Dark mode is the default; light mode activates via `@media (prefers-color-scheme: light)`.

| Token          | Dark             | Light           | Purpose                      |
|:---------------|:-----------------|:----------------|:-----------------------------|
| `--bg`         | `#050505`        | `#f5f5f5`       | Page background              |
| `--fg`         | `#e0e0e0`        | `#111`          | Primary text                 |
| `--fg-dim`     | `#333`           | `#ccc`          | Cell borders, muted elements |
| `--alert`      | `#FF003C`        | `#FF003C`       | Accent / alert color         |
| `--cell-min`   | `280px`          | `280px`         | Min cell width on mobile     |
| `--gap`        | `6px`            | `6px`           | Grid gap                     |
| `--radius`     | `2px`            | `2px`           | Border radius                |
| `--font-mono`  | `"Anonymous Pro"` | same           | Monospace font (slashed 0)   |

### Layout (`src/layouts/Layout.astro`)

- Loads **Anonymous Pro** from Google Fonts (slashed zeros on all devices)
- Global box-sizing reset
- `font-feature-settings: "zero"` as fallback 4 slashed zeros
- Scanline overlay via `body::after` (subtle CRT effect, disabled on touch devices)
- On desktop (900px+): viewport-locked (`overflow: hidden; height: 100dvh`)

### Grid (`src/pages/index.astro`)

The bento grid is 1 column by default, then switches 2 2 columns at 580px+, 3 columns at 900px+, and 4 columns at 1200px+.

**4-column grid mapping (1200px+):**
- **Cell A** (Felipe_Galind0_w / StatusCard): columns 1–2, rows 1–2
- **Cell B** (yt_playlist_w / PolycamGallery): columns 3–4, row 1
- **Cell C** (gh_stats_w / GithubStats): column 3, row 2
- **Cell D** (blog_w): column 4, row 2
- **Cell E** (footer_w): columns 1–4, row 3

**3-column grid mapping (900px+):**
- **Cell A** (Felipe_Galind0_w / StatusCard): column 1, rows 1–2
- **Cell B** (yt_playlist_w / PolycamGallery): columns 2–3, row 1
- **Cell C** (gh_stats_w / GithubStats): column 2, row 2
- **Cell D** (blog_w): column 3, row 2
- **Cell E** (footer_w): columns 1–3, row 3

**2-column grid mapping (580px+):**
- **Cell A** (Felipe_Galind0_w / StatusCard): column 1, row 1
- **Cell C** (gh_stats_w / GithubStats): column 2, row 1
- **Cell B** (yt_playlist_w / PolycamGallery): columns 1–2, row 2
- **Cell D** (blog_w): column 1, row 3
- **Cell E** (footer_w): column 2, row 3

### JavaScript (minimal)

Only 2 inline scripts exist, both intentionally tiny:

1. **HTTPS redirect** in [src/layouts/Layout.astro](src/layouts/Layout.astro)
   - Checks `location.protocol` and redirects 2 HTTPS in prod (keeps `localhost` HTTP 4 dev).
   - Necessary 2 avoid mixed-protocol deploys without server-side redirects.
2. **Polycam iframe boot** in [src/components/PolycamGallery.astro](src/components/PolycamGallery.astro)
   - Sets the iframe `src` once, adding `controls=0` only on desktop-class pointers.
   - Necessary 2 keep 1 iframe while hiding controls on desktop and keeping controls on touch devices.

Everything else is static HTML/CSS at build time.

### Components

#### StatusCard (`src/components/StatusCard.astro`)
- GitHub avatar image (circular, responsive via `clamp()`)
- Name, tagline, list of accomplishments
- Version display from `src/config.ts`
- No JS in this component

#### PolycamGallery (`src/components/PolycamGallery.astro`)
- Embeds a YouTube playlist via a single `<iframe>`
- Inline script sets the `src` w/ minor controls logic (see JavaScript section)
- Playlist ID is hardcoded in the component

#### GithubStats (`src/components/GithubStats.astro`)
- Uses [github-readme-stats](https://github.com/anuraghazra/github-readme-stats) via a self-hosted Vercel app
- `<picture>` element with `<source>` 4 automatic light/dark theme switching
- Transparent background, theme-matched text colors
- Stats API URL pattern: `https://github-readme-stats-sigma-five.vercel.app/api?username=USERNAME&show_icons=true&bg_color=00000000&text_color=COLOR&icon_color=COLOR&title_color=COLOR&hide_border=true`
- Links 2 GitHub profile on click

### Post-Build Script (`scripts/inject-size.mjs`)

Replaces the `__SIZE__` placeholder in `dist/index.html` with the actual file size in KB. The StatusCard currently hardcodes `~4KB`, so the placeholder does nothing until the template switches 2 `__SIZE__`.

---

## Quick Start

```bash
# Clone
git clone https://github.com/Felipegalind0/felipegalind0.io.git
cd felipegalind0.io

# Install
npm install

# Dev server (localhost:4321)
npm run dev

# Build (outputs 2 dist/)
npm run build

# Preview built site
npm run preview
```

---

## Customization Guide

### Use as a template 4 your own site

1. **Fork or clone** this repo
2. **Update `public/CNAME`** with your domain (or delete it 4 `username.github.io`)
3. **Update `astro.config.mjs`** — change `site` 2 your URL
4. **Edit `src/components/StatusCard.astro`:**
   - Replace the avatar URL: `https://github.com/YOUR_USERNAME.png?size=80`
   - Change the name, tagline, and accomplishment lines
5. **Edit `src/components/GithubStats.astro`:**
   - Replace `Felipegalind0` with your GitHub username in both image URLs
6. **Edit `src/config.ts`:**
   - Update the `VERSION` string
7. **Customize tokens** in `src/styles/tokens.css`:
   - Change colors, gap, radius 2 your liking
8. **Change the font** in `src/layouts/Layout.astro`:
   - Swap the Google Fonts `<link>` tag and update `--font-mono` in tokens.css

### Adding a new widget

1. Create `src/components/MyWidget.astro`:
   ```astro
   <div class="my-widget">
     <!-- your content -->
   </div>

   <style>
     .my-widget {
       /* scoped styles */
     }
   </style>
   ```

2. Import and place it in `src/pages/index.astro`:
   ```astro
   ---
   import MyWidget from "../components/MyWidget.astro";
   ---
   ```
   ```html
   <section class="cell cell-b"><MyWidget /></section>
   ```

3. Adjust grid placement in the `<style>` block of `index.astro` if needed.

### Placeholder widgets (not yet implemented)

| Cell | Name            | Suggested purpose                   |
|:-----|:----------------|:------------------------------------|
| D    | blog_w          | Random/glitch art, fun data         |
| E    | footer_w        | Latest GitHub activity feed         |

---

## Deployment

### GitHub Pages (current setup)

The site deploys 2 the `gh-pages` branch using the `gh-pages` npm package:

```bash
# Build + deploy in one command
npm run build && npx gh-pages -d dist
```

This pushes the `dist/` folder 2 the `gh-pages` branch. GitHub Pages serves from that branch.

**Important:** GitHub Actions deploy was attempted but disabled due 2 billing restrictions. The manual `gh-pages` approach works without Actions.

### Custom Domain

1. Set DNS A records (DNS-only, no proxy) pointing 2 GitHub Pages IPs:
   ```
   185.199.108.153
   185.199.109.153
   185.199.110.153
   185.199.111.153
   ```
2. Add a CNAME record 4 `www` → `username.github.io`
3. Keep `public/CNAME` with your domain — it gets copied 2 `dist/` on build
4. In repo Settings → Pages, verify the custom domain

### Other hosts

Since this is a static site (`dist/` is plain HTML/CSS), it works anywhere:
- **Netlify / Vercel / Cloudflare Pages:** Connect repo, build command `npm run build`, output dir `dist`
- **Any web server:** Just serve the `dist/` folder

---

## Commands

| Command                    | Action                                    |
|:---------------------------|:------------------------------------------|
| `npm install`              | Install dependencies                      |
| `npm run dev`              | Start dev server at `localhost:4321`       |
| `npm run build`            | Build 2 `./dist/` + log page size         |
| `npm run preview`          | Preview built site locally                |
| `npx gh-pages -d dist`     | Deploy `dist/` 2 `gh-pages` branch        |

---

## Design Principles

- **Minimal JS** — 2 small inline scripts only (HTTPS redirect + Polycam iframe boot)
- **System theme** — respects `prefers-color-scheme`, no manual toggle
- **Tiny** — size is injected at build time when `__SIZE__` is used in the template
- **Monospace** — Anonymous Pro with slashed zeros everywhere
- **Bento grid** — responsive CSS Grid, no layout frameworks
- **Scanline overlay** — subtle CRT texture on desktop, disabled on mobile

---

## Tech Stack

- [Astro](https://astro.build) v5.17.1 — static site generator
- [Anonymous Pro](https://fonts.google.com/specimen/Anonymous+Pro) — monospace font with slashed zeros
- [github-readme-stats](https://github.com/anuraghazra/github-readme-stats) — live GitHub stats SVG
- YouTube iframe — Polycam playlist embed
- CSS Grid + custom properties — no frameworks, no preprocessors
- GitHub Pages — hosting

---

## Quick Reference

- **Build:** `npm run build` → static output in `dist/`
- **Deploy:** `npm run deploy`
- **Layout diagrams:** [scripts/update-layout.py](scripts/update-layout.py) reads [src/pages/index.astro](src/pages/index.astro)
- **Minimal JS sources:** [src/layouts/Layout.astro](src/layouts/Layout.astro), [src/components/PolycamGallery.astro](src/components/PolycamGallery.astro)
- **Version string:** `src/config.ts`

---

## License

MIT

### Manual GitHub Pages redeploy

If the GitHub Pages "pages build and deployment" workflow does not trigger after a deploy, you can force it by running:

```bash
npx gh-pages -d dist --dotfiles --no-history
```

This will force-push the latest build 2 the `gh-pages` branch and retrigger the GitHub Pages workflow. Make sure `.nojekyll` is present in `dist/` so the `_astro/` directory is not ignored.

You can also use the shortcut:

```bash
npm run deploy
```

which runs the correct command with `--dotfiles` included.

---

## Version History

| Version | Date       | Changes                                          |
|:--------|:-----------|:-------------------------------------------------|
| v.3.0   | 2026-02-18 | Version bump 2 v3.0 (see `src/config.ts`)        |
| v.2.1   | 2026-02-11 | Polycam 3D scan gallery (YouTube playlist embed) |
| v.2.0   | 2026-02-11 | Source link in footer, versioning, flattened repo structure |
| v.1.0   | 2026-02-11 | Anonymous Pro font (slashed zeros), comprehensive README |
| b.0.1   | 2026-02-10 | Initial build: bento grid, StatusCard, GithubStats, deployment |
